#include "main.h"

/**
 * _islower - Checking of lowercase character
 * @c: character to check for the code
 * Return: 1 if c is lowercase, 0 0therwise
 */
int _islower(int c)
{

return (c >= 'a' && c <= 'z');
}
