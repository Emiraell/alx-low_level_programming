#include "main.h"
/**
 * print_alphabet - Entry point of the alpahbets
 */
void print_alphabet(void)
{
char alpha;
alpha = 'a';
for (alpha = 'a'; alpha <= 'z'; alpha++)
{
_putchar(alpha);
}
_putchar('\n');
}
